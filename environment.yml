# environment.yml
name: geo_sat_env
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.12
  - pip
  - numpy
  - scipy
  - matplotlib
  # Optional: For GPU acceleration if needed later
  # - cudatoolkit=12.5 
  # - pytorch::pytorch 
  - pip:
    # Core library for Geometric Algebra
    - clifford
    # Tools for 3D visualization (optional but highly recommended for geometry)
    - vedo  
    - trimesh
